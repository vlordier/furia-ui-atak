# Local only — never commit a real value.
# Figma personal access token / OAuth secret (not the community file id).
FIGMA_ACCESS_TOKEN=
# Optional: your private /design/ file key for MCP (not an API secret, still keep out of git).
FIGMA_ATAK_FILE_KEY=
